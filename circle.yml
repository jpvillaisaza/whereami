dependencies:

  cache_directories:
    - elm-stuff/build-artifacts/

  pre:
    - npm install --global elm

  override:
    - elm make --yes

test:

  override:
    - elm make WhereAmI.elm --output=whereami.js --warn
